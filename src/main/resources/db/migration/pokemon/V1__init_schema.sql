-- Bootstrap migration for the pokemon schema.
-- Intentionally a no-op placeholder: proves the Flyway/schema wiring works
-- end-to-end before any pokemon persistence need exists. Add real DDL as V2+.
select 1;
